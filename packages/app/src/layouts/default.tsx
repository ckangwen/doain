import { GlobalLayout } from "@effective/client";
import { defineComponent } from "vue";

export default defineComponent({
  name: "GlobalDefaultLayout",
  render() {
    return <GlobalLayout></GlobalLayout>;
  },
});
